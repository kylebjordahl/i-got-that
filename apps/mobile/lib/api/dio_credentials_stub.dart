// Native (non-web) stub — see dio_credentials.dart. Native has no cookies;
// sessions travel via the Authorization header instead.
import 'package:dio/dio.dart';

void enableSessionCookie(Dio dio) {}
